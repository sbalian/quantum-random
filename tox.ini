[tox]
isolated_build = True
envlist = py37, py38, py39, py310

[testenv]
whitelist_externals = poetry
commands =
    poetry build
    poetry install -v
    poetry install -v -E numpy
    poetry run isort . --check-only
    poetry run black --check .
    poetry run flake8 .
    poetry run mypy qrandom
    poetry run pytest
